# 历史 ARR 副本

2026-09-16 从本机相邻旧版项目 `data_gen_v6/.arr_gen` 补回 80 个 `Pos1Azi1freq*.arr` 和四个 `template_pos1` 模板，共 42,021,328 字节 ARR 数据。

文件头中的计算几何：源深 5/10/15 m，接收深度 500/1000/1500 m，水平距离 1/2/3/4 km。这些文件保留作历史资料及可能的旧实验复用，不能作为当前主线的互易 13 距离信道输入。

此前被删除的文件没有保留哈希，不能声称这份副本是对原删除文件的逐字节恢复。当前主线只读取 `acoustics/arr` 中按现行配置生成的 `ReceiverAzi1freq*.arr`。
